# 台股滾動10日跌幅系統 v14
# Streamlit Cloud 部署用 requirements.txt
# 依據 app.py 實際 import 的第三方套件反推產生（含函數內延遲 import）
# 全部使用 >= 開放版本（與原本能跑的策略一致，不激進釘死，降低相容性風險）

# ── 核心框架（app 啟動即載入，缺一不可）──
streamlit>=1.32.0
pandas>=2.0.0
plotly>=5.18.0

# ── 資料抓取 ──
yfinance>=0.2.38
requests>=2.31.0

# ── PDF 匯出模組（app.py 的 _pdf_* 函數使用，原本 requirements 漏列！）──
reportlab>=4.0.0

# ── 每日市場簡報 Tab 專用 ──
feedparser>=6.0.11          # RSS 新聞抓取
deep_translator>=1.11.4     # 英文新聞自動翻譯中文（主力）
googletrans>=4.0.0rc1       # 翻譯備援（app.py 內有 try/except 保護，缺此套件不會使 app 崩潰）
